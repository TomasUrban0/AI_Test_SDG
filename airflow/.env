# =============================================================================
# Variables de entorno para docker-compose
# =============================================================================
# AIRFLOW_UID: debe coincidir con tu usuario de Linux/macOS.
# En Linux: ejecuta "id -u" para obtenerlo.
# En macOS/Windows (WSL2): generalmente 50000 funciona.
AIRFLOW_UID=50000
