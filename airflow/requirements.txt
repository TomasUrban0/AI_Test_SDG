# =============================================================================
# Dependencias Python para el pipeline de churn dentro de Airflow
# =============================================================================
# Estas se instalan en la imagen custom de Airflow (ver Dockerfile)

# --- Core ML ---
pandas==2.1.4
numpy==1.26.4
scikit-learn==1.4.2
xgboost==2.1.4
lightgbm==4.5.0
shap==0.46.0

# --- Extensión 3.6: PostgreSQL ---
psycopg2-binary==2.9.9
# sqlalchemy: NO pinear — Airflow 2.8.1 ya incluye una versión compatible (1.4.x)
# Pinear sqlalchemy>=2.0 causa: ArgumentError: Invalid value for 'executemany_mode'
